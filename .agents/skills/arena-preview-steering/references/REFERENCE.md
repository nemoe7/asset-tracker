# Preview transport: operation and migration

## Runtime contract

The shared runtime is `scripts/preview.py`, relative to the steering skill. It uses Python 3.10+ standard-library HTTP, JSON and SQLite support. The report renderer imports `markdown-it-py` only when rendering, and `serve` checks it at startup, exiting with the install command when it is missing, since a page that cannot render Markdown is worse than no page; the CLI commands need no renderer. The reporting skill is a separate entry point, installed beside steering, not an independent copy of the server.

The chosen `--state-dir` contains `state.sqlite3`. Normal SQLite transactions handle concurrent browser sends and CLI receipts; no message is marked acknowledged by a read, and no path other than `read` marks one seen. Writes are committed before the server returns success. Notes retain IDs, server timestamps, text, optional acknowledgement timestamps and an optional acknowledgement kind, `note` or `reply`, with its answer text. Report answers are recorded in a separate `submissions` table with the same receipt columns plus their report ID: `read` merges both as pending items tagged `kind: note` or `kind: report`, `ack` accepts either ID, and `/api/state` serves notes only, so submissions never render as messages. Reports retain stable IDs, titles, Markdown snapshots, update timestamps and a `seq` fixed at first publish, which is the send order the UI numbers; their fields are parsed from that snapshot at every render, never stored separately. Both additions migrate in place: missing columns are added as nullable, `seq` is backfilled from `rowid`, and no message or report is dropped. Keep this directory Git-ignored and outside transient cache/build directories.

| Command | Purpose |
| --- | --- |
| `init` | Create state without starting HTTP |
| `serve` | Start the shared interface on `0.0.0.0`, port 8000 unless `--port` says otherwise |
| `read` | Print all unacknowledged messages; record check time; stamp `seen_at`; minified JSON |
| `task ID TITLE [DETAIL ...]` | Add or update one task in the Tasks tab; `--status`, `--order`, or the `--task-*` flags |
| `task-remove ID` | Delete one task and echo what was stored |
| `task-import [FILE]` | Rebuild the list from JSON, a file or stdin; `--replace` clears first; note lines are skipped |
| `task-list` | Print every task as minified JSON, in the shape `task-import` reads back |
| `ack <id> [<id> ...] --reply <markdown>` | Record receipts with a rendered answer in the log |
| `ack <id> [<id> ...] --note <text>` | Record receipts with one plain answer line |
| `publish <source.md> --id <id> --title <title>` | Add/update a report snapshot |
| `import-notes <notes.ndjson>` | Import ID/text/time records, plus the receipt a line carries and its read stamp, written verbatim; a report answer line is restored in the same run, with its own receipt and read stamp; task lines are skipped; a partial receipt is refused and none is invented |

All commands take `--state-dir` before the subcommand, and `--save-path` selects where the save button writes its file, `saved-state.ndjson` at the repository root by default; JSON printed by this CLI is minified because the agent pays for the whitespace it reads. `--pretty`, also before the subcommand, indents that JSON for a human eye. Commands that read existing state fail if the database is missing; they do not create a misleading empty inbox. Import is idempotent by ID and rejects an existing ID with different text. It does not import ntfy messages or claim they were acknowledged.

The UI sends the same client-generated ID on an unchanged retry. It distinguishes saving, confirmed storage and explicit agent acknowledgement; it never interprets an HTTP request or an inbox check as a chat acknowledgement. Errors preserve the draft. Switching views preserves the mounted composer. The user can send with Enter, use Shift+Enter for a newline, browse message history — each entry shows its ID and, once acknowledged, the agent's reply rendered like a user message or its plain note — pick a numbered report in send order, answer a fielded report in the Reports tab and switch dark/light themes. Field options render as text nodes; an unconfirmed submission keeps the user's input and says so. The default dark palette was supplied by the owner from Arena's UI.

The page reads as a chat, by owner direction on 2026-09-20. It carries no `h1`; a pinned top bar holds the tabs, the theme button and a toggle that collapses the bar to that button alone. The message log sits above the composer, oldest first, fills the viewport between them and scrolls in place, holding its position when the user has scrolled up and pinning to the newest message otherwise. A second toggle hides the composer and gives the log the remaining height. Both toggles persist per browser like the theme, and reopening the composer pins the log to the newest message. Rendered reports keep their own headings, so the report `h1` and `h2` spacing is unaffected. The composer textarea carries no resize handle and grows with the draft instead, on owner direction: its height is set from `scrollHeight` on every input, on load with a restored draft, when the Markdown preview closes and when the composer reopens, so the box expands upward and the log gives way. Nothing caps it — the 48% composer ceiling and the composer's own scrollbar are gone — so when the pair no longer fits, the notes panel scrolls as a page, which is how the log stays reachable by scrolling up; the log card keeps a 200px floor so it cannot be squeezed out of existence, and its list still scrolls in place inside that floor. A P1 report followed on that first attempt: with the layout on `min-height: 100%` the log card was unbounded, growing to its content and defining the page height instead of being squeezed, so the composer's tail was not anchored and the box read as expanding downward. `height: 100%` is back, which bounds the layout to the panel — the composer's bottom edge sits at the viewport bottom, growth pushes the log upward, and the panel's scrollbar appears only when the composer alone exceeds what the log card's 200px floor leaves it. The textarea also hides its own overflow, and growth adds 2px to `scrollHeight` for the 1px border that `scrollHeight` excludes under `box-sizing: border-box`; without those 2px the box was set 2px short and the browser answered with a scrollbar inside it. The composer's placeholder is half as prominent as the muted face it is written in, `opacity: 0.5` on `textarea::placeholder`, on the owner's note `91f42108`, so a hint about what could be typed is distinguishable both from what has been sent and from what is being typed. The note asked to halve the placeholder's transparency; halving its prominence is the reading that serves the reason given, since the literal opposite would push a hint toward looking like text, and the ack says which way it went so one word reverses it.

Three refinements followed on owner notes the same day. The bar toggle is a chevron at the right end of the bar, ▲ while open and ▼ while closed, and a closed bar shrinks to a thin strip carrying only that button, right-aligned, rather than keeping its full height; the theme button shares that sizing and reads ☀ in dark mode and ☾ in light mode, with the accessible name still spelling out the target mode so the glyph is never the only label. The clock sits in a tight row with that button so the pair stays at the right end of the bar instead of being spread across it, takes the tab font size rather than the muted one, and shows the log's own face with seconds appended, as in `Sep 21, 20:52:31`, refreshed once a second with tabular numerals so the width never changes; the full weekday and date stay in its hover title. The reports tab carries one copy button, and what it copies is chosen for what exports it: the shown report's Markdown source, fetched from `/api/reports/<id>/source` so nothing extra rides the poll, before its refresh button. The log and the tasks had one each, and both are gone on owner note `586d52f7`: the file the save button writes is the restore path now, so a second copy of the same data only made two things to keep in step. Instead the save button copies on a shift-click: the page's cached state as minified JSON, which is exactly the object the button posts, so one gesture puts a pasteable copy on the clipboard without exposing a path or a file. The log copy carried the receipt keys and the task marker, and a note line in the saved file still does, since that is what `import-notes` reads. A button ticks green with the clipboard path it took, or crosses red when the browser allowed neither path or there was nothing loaded to copy. Vertical space is reclaimed by stacking: the log header puts its title, connection line and last-check line in one left-aligned block that shares a row with the refresh icon, named `Refresh messages`, and the composer toggle, and the composer puts the Enter/Shift+Enter instruction above the send status in the same left-aligned stack, sharing a row with Send note. The Markdown preview toggle reads `MD 👁` and dims while the preview is closed, lighting up while it is open, inverted on owner direction so a lit button means the preview is showing; the pencil keeps the opposite mapping, grayed while the composer is hidden, because a hidden composer is the state worth marking there; the composer toggle is a pencil, grayed while the composer is hidden; the log refresh is an icon. Icon buttons are 34px squares with a 19px glyph, centered, and keep that size whether the bar is open or closed, after the owner reported them resizing; both refresh buttons are icons, the log one named `Refresh messages` and the report one `Refresh report`. The report refresh is a 44px square instead of 34px, on owner direction, so it stands as tall as the report select beside it in the bottom-aligned toolbar; the log icons keep their 34px squares and its 19px glyph is unchanged, which leaves the ↻ proportionally smaller in the larger box. Focus shows as a border colour change rather than an outline, on owner direction to match Arena, which costs keyboard users the ring on elements that carry no border. In dark mode the focus border is `#524d47`, chosen by the owner over the `#f4ca93` it replaced and close to the `#56504a` resting border, so a focused field is only faintly distinguishable from a resting one; the light theme keeps its `#8f5b13`, which the owner's note did not name. `check_preview.py` pins both values in the served page. The composer card keeps 8px of vertical padding and its rows 6px, so row spacing carries the rhythm instead of stacking on card padding. The log header row carries no vertical margin and its card no vertical padding beyond 12px. The connection line carries a dot in place of the word `Connected`, on owner direction: green when the last poll of `/api/state` returned 200, red when it failed, gray while connecting, with `role="img"` and the label `Connected` or `Disconnected` so the state a word used to carry stays available to a screen reader. The dot reports the page's reach to the server only; the agent's presence is the last-check line beside it. The composer holds the textarea, that preview toggle, Send note, that stacked instruction and status pair, and one header, after the owner called the rest bloat: the intro line, the draft-status line and the notice paragraph are gone, the section keeps `aria-label="Send a steer"` as its name, and a browser-storage failure is reported on the send-status line instead of a paragraph of its own. Timestamps are 24-hour by default, on the single formatter every receipt shares, and drop the seconds; the year appears only when it differs from the current year, and then as two digits. 12-hour time or a four-digit year returns only on an explicit owner ask. Rendered Markdown keeps one heading rhythm: every level from `h1` to `h6` inside a report takes 10px above and 6px below, the report's first child stays flush to the top, and The paragraph gap was the browser's 16px while the headings were already tight, and the owner called the paragraphs too far apart (note `9454e0b1`): a paragraph in a report, in a rendered message or in the draft preview now takes 8px above and below, so two of them sit 8px apart and the first one stays flush. a fenced code block takes 10px of padding, all on owner direction that the gaps were too large and uneven, the lower levels being the worst offenders because only `h1` and `h2` had been tightened before and `h3` to `h6` still fell back to browser defaults. A bug report on the sizes followed: `h2` carried an explicit 19px while an unstyled `h3` computes to the browser's 1.17em, 18.72px on a 16px root, so two levels read as one size. Four explicit sizes went in for `h3` to `h6` and came back out again, and then `h1`'s clamp and `h2`'s 19px went out too, on the owner's direction that every heading can be left at an unset font size because the defaults look fine: the scale is now 32, 24, 18.72, 16, 13.28 and 10.72px, which descends cleanly and removes the collision at its source instead of patching one number. `h1` keeps its -.035em letter-spacing and `h2` its margins, the notes having named font size only, and `check_preview.py` pins both rules and the absence of any heading font size in the served page. The cost is that a report's `h1` no longer scales with the viewport, since `clamp(25px, 4vw, 34px)` left with the rest; it sits at a fixed 32px. Inline `code` outside a `pre` carries a chip: 1px of vertical and 5px of horizontal padding and a 4px radius, on owner direction that code elements should have a background by default. Its colour is `--code-bg`, which is `#1b1a19` in dark mode on owner direction — darker than the `#252523` page, so the chip reads as recessed rather than raised on the `#413c3a` bubble it replaced — and stays the bubble in light mode, since only one dark hex was named. A separate tone was introduced for that chip and then dropped on owner direction, so the bubble is back and no chip variable is left in either theme. A follow-up note made the rule global instead of scoped to `.report`, so a raw message, a field option and the draft preview chip the same way a published report does, with the message-log receipt IDs as the one exclusion: `code.note-id` keeps the monospace face and drops the background and padding, since a chip under every ID would be noise. A click on one copies the whole ID rather than the seven characters on show, through the same clipboard path a code block uses, and the ID takes the pointer cursor and a brief green or red as the only sign that anything happened. The global selector is two type selectors, so any class rule outranks it, which is what makes that exclusion work without `!important`. That same rule fixes the reported bug where a one-line fence had no background, because markdown-it renders a fence closed on the line that opened it as an inline code span inside a paragraph rather than as a block, so it never reached the `pre` background; code inside a `pre` keeps no background of its own, the block carrying it, and that rule is not scoped to a report, because a fence in a message or in the draft preview is the same block and the owner read a missing background there as the bug (notes `850de5a1`, `4b57f95a`). A fence the owner escaped with a backslash before each marker opens a block too, on either marker, backtick or tilde: the line-leading escape is dropped before the render, because markdown-it otherwise reads a literal ``` inside a paragraph and the background rule never reaches it (owner note `850de5a1`). An escape anywhere else is left alone, so inline text keeps its literal backticks. Every code block also carries a copy button, injected by the renderer rather than by the client, and it is an icon rather than a word: `add_copy_buttons()` wraps each `pre` in a `div.code-block` with a `button.copy-code` before it, holding the code in `data-code` with newlines as `&#10;`, because rendered HTML is assigned through `innerHTML` and so cannot carry a listener of its own. One delegated click handler on `document` reads that attribute, tries `navigator.clipboard.writeText`, and on any failure falls back to a hidden textarea with `document.execCommand('copy')`, showing `⧉` at rest, `✓` for 1.5 seconds on success and `✗` tinted the connection dot's red when both are blocked, the words having moved into `aria-label` and a `title`. That title names the path that worked, `Copied through the clipboard API` against `Copied through the selection fallback`, so hovering settles the iframe question instead of leaving it a hypothesis. The button sits in the block's top-right corner, which is why a `pre` inside a code block keeps 40px of right padding, the button being a 26px square, and the clipboard can be denied in a framed preview, so the fallback is the path a sandbox user is most likely to take. The receipt's monospaced ID sits outside any `.report` element, so it takes no chip. Read state for a report is stamped on the report itself rather than kept in the tab's storage: reaching the end of the panel that scrolls marks it read at once, and a report that fits the panel with nothing to scroll is marked read after five seconds in view, on owner direction, so a flick past a short report stamps nothing and leaving the tab before the dwell ends stamps nothing either. An update to the report being read keeps the panel where the owner left it, because the panel is the element that scrolls and the report is replaced inside it, and a report the owner switches to starts at its own top, as the owner asked on note `60cdef88`. The harness models that browser clamp on the panel, so a missing restore fails the client check instead of passing unexercised. The browser writes the stamp through `POST /api/reports/<id>/seen`, which is token-protected like the other writes and keeps the first look rather than moving it on every open, and the stamp is folded into the page's state rather than re-fetched, so reading a report to its end moves nothing; what depends on the stamp is the pip on the Reports tab, which shows while any report carries none, and the report select, whose label marks an unseen report with an asterisk, `1. * Fielded` against `1. Fielded`. Republishing clears the stamp, so a report whose text changed is unread in fact rather than unread by a comparison the client has to get right, and a cleared browser cannot make a read report unread again because the stamp never lived there. 
Notes, agent replies and the composer's draft preview render with `breaks=True`, so a single newline becomes a `<br>` the way a chat message reads. Published reports keep standard CommonMark, where a single newline is a soft break that collapses to a space, so authored report layout does not shift. Before this change a note typed with single line breaks arrived in the log as one continuous line, which the owner reported as a bug.

## The Tasks tab

The owner asked for a live agent task list as its own tab rather than a report, in two divs: finished tasks above, upcoming below. A third div was added on the owner's suggestion `99928e9a`, and note `a3002b9f` then reversed the order of the two stored ones, so the panel reads Current, Upcoming, Finished: the head of the queue leads, what is coming follows it, and the finished record — the longest list of the three, and the one nobody is waiting on — sits last instead of pushing the other two below the fold. `tasks-current` holds the head of the queue, `tasks.upcoming[0]`, so what the agent is on next is readable without scanning a list, and it hides when the queue is empty. That task stays in Upcoming as well — removing it would leave a hole in the authoritative order, and the copy button hands both stored sections over as they are — so the duplication is deliberate and not an oversight. One task is one record, written by a command rather than formatted as Markdown, because the agent pays tokens for formatting:

```
preview.py task docs-archive "Move to new docs/archive/ dir" \
  "move BUDGET-EXCEPTIONS.md to docs/archive/budget-exceptions.md" \
  "write docs/archive/arena-quirks.md"
```

The first three positionals are the ID, the title and any number of detail lines, and the same three are available as `--task-id`, `--task-title` and a repeatable `--task-details`. A task ID is 1-64 characters of lowercase letters, digits and hyphens; a title is 200 characters at most; a task carries 40 details of 2000 characters each. Writing an ID that is already stored updates it, and a title or details left out keep their stored values, so `task docs-archive --status finished` moves a task between the divs without repeating it. `--order N` places a task at that 1-based position inside its div instead of at the end, positions stay dense after every insert, move and removal, and `--task-details ""` clears the detail list. `--msg-id <message id>` names the message the task answers and marks that message in the log. The task and the marker share one transaction, so a message ID that matches nothing is refused and leaves no task behind. A message ID may belong to a note or to a report submission, as with `ack`. `--status` is `upcoming`, the default, or `finished`. Each command prints JSON of what it stored, with every detail cut to 200 characters in the echo only, since the echo is a confirmation and the stored value stays whole. `task-remove ID` deletes one task and echoes it, and `task-list` prints all of them. `--amend PREV-ID` renames a stored task to the ID on the command line and keeps its title, details, status and position, so a malformed ID costs no deletion, and it refuses rather than overwrite when the new ID is already stored. The echo carries `prev` and `next`: the IDs either side of the task inside its own div, or null at the ends, which is enough to place the next one without a `task-list` call. `task-import` reads a JSON array of records, or one record per line, from a file or from stdin, and rebuilds the list; it merges by ID unless `--replace` clears the table first. Together with `task-list`, or the copy button on the tab, that is a backup and restore pair for a sandbox reset.

The records live in a `tasks` table, so `/api/state` carries them and the tab refreshes on the poll the log already makes, with no endpoint of its own. The browser builds each row from the record with `textContent`, which is why nothing here is rendered to HTML on the way in and a hostile title stays text. A row shows its title and, in the muted face below it inside the collapsible block, its details as a real `<ul>` — the owner's note `11a35a0f` — so each detail carries its own marker and reads as a separate statement rather than a line inside one span; the same details are still joined into the row's hover title. Before the first write the panel says so in words instead of showing three empty divs. The tab takes no answers: it is the agent's status rather than a question, so nothing shown there arrives in the inbox. Unlike a report, a task list needs no Markdown renderer, so `task` works even where `serve` would refuse to start.

## The Uploads tab

A fourth tab carries files into the sandbox, and the owner answered its four open questions in report
submission `c27a4dd5`, so it is built to those: the bytes go to `uploads/` beside the database rather
than into it, one file is 1,000,000 bytes at most, any bytes are accepted while the content type, the
size and the SHA-256 hash are recorded, and a record outlives its bytes because a restore removes what
is under the state directory. The tab says that last part in words rather than leaving a row that opens
nothing: `present` rides `/api/state` and a row whose file is gone reads `the bytes are gone; the record
survived a restore` in place of its path.

`POST /api/uploads?name=<file name>` carries the file itself as the request body, so the name rides the
query string and the content type is the browser's; the route is token-protected like every other write
and is the one write exempt from `application/json`, since the body is the file. `MAX_UPLOAD` is
1,000,000 bytes, the same ceiling the report importer uses, and a longer body is answered `413` before
it is read. The bytes never enter the database: `Store.save_upload` writes the file first and then the
row, the row keeps the file name under `UPLOAD_DIR` rather than an absolute path so a state directory
that moves still resolves, and the stored file is named after the upload's own UUID with the owner's
extension kept, so no name the owner types can escape that directory. A file the browser gives no type
for goes as `application/octet-stream`.

The row shows the name, the size in bytes, the type, the time, the first twelve characters of the hash,
and the stored file under `uploads/` — a path rather than a download link, because the preview offers no
download controls on the standing owner choice recorded above, and what an upload is for is handing
bytes to the agent that can read them from the state directory. The client refuses an empty file and one
over the ceiling before anything leaves the page, clears the input after a successful send, and prints
the server's own refusal when a send fails. `check_preview.py` pins the tab, its input and its list in
the served page, and the sentence the tab shows about a restore, and the harness covers the ceiling, the
byte-exact body and the record whose bytes are gone.

## Fields in a report

The renderer splits a source into prose blocks and fields before rendering. A field is written as Markdown:

| Marker | Control | Notes |
| --- | --- | --- |
| `- ( ) option` list | Radio group | `- (x)` preselects that option |
| `- [ ] option` list | Checkbox group | `- [x]` preselects that option |
| `- ( ) Label: ___` inside a group | Free-text slot | The typed text becomes the answer, as `Label: text` |
| `Label: ___` or a bare `___` line | Text box | At most 2000 characters |

A report may hold 1 to 50 fields, and each text answer is 2000 characters at most, so a fully
answered report can legitimately carry far more than the 4000 characters a note is capped at. Its
submission therefore has a bound of its own, 150,000 characters, checked before anything is stored,
and it refuses with the limit named in the error rather than truncating an answer the owner typed.
That bound was added after a review found the note cap being applied to the built `REPORT` message,
which rejected a valid fully-answered report with a 400; a note keeps its own cap and its own
wording.

Consecutive marker lines of the same kind form one group. The prompt is the label before `___`, else the nearest non-empty
line above the group, stripped of list, heading, quote and emphasis markers and a trailing colon. The field ID is a slug of
that prompt, deduplicated with a numeric suffix; `{#my-id}` at the end of the prompt line sets it explicitly and is removed
from the rendered text. Markers inside fenced code blocks are literal text. Limits: 1–50 fields, prompts
1–500 characters, and 1–20 unique options of 1–200 characters, where a group may hold a single option. A duplicate option in one
group is an error, and the whole report then fails to render rather than silently dropping a choice. An option written as a label followed by `___` — `Other: ___`, or a bare `___` — is a free-text slot rather than a fixed choice, the same shape that makes a whole line a text box: it renders its radio or checkbox beside a one-row textarea of at most 2000 characters that grows with what is typed the way the composer does, carrying the label as its placeholder rather than printing it in front of the box, on owner direction that the printed label was noise. Both controls keep an accessible name spelling the label out, because a placeholder disappears once typing starts. Typing in a slot checks it and blanking the text lets go again, so the box follows the words; the answer is still the label, a colon and what was typed, trimmed. A slot that is checked with nothing typed contributes no answer at all, so an empty slot never reaches the inbox as noise, and two slots in one group may not share a label or the report fails to render.

`GET /api/reports/<id>/html` returns JSON with `html` and the field count, not raw HTML. `POST /api/reports/<id>/submit` pairs a
client-generated ID with per-field answers (text at most 2000 characters, `choice` one of its options or a free-text slot's
`Label: text` with 1–2000 characters typed, `checkbox` a unique subset of its options and slots) and writes one inbox note headed `REPORT <id> <title>:`, one indented line per field, `(skipped)` for absent or empty
answers. Resending is a new answer, not an update. A successful send also stores `{answers, at}` in browser storage under `answers:<report id>`; loading that report again pre-fills its controls from that record — a free-text slot comes back with its box checked and its typed text in its textarea, grown to fit, and an answer that moved off the slot clears it — and shows a `✓ Sent <time>` receipt beside the button, so the user can amend and resend. The status line carries the same fact and sits inside `.report-toolbar` in the muted 13px face, on owner direction that it should not be misread as part of the report: `Answers sent <time> · N fields stay filled in` once a record exists for the selected report, `Report loaded with N fields` before one does, and it takes its own row in the toolbar through `flex: 1 1 100%`. The send message shares the one timestamp formatter, so it no longer prints seconds or a 12-hour clock. The record is origin-local display state, never a server-side answer history: another browser shows the report's Markdown defaults. A report with no fields rejects a submission. Republishing a source does not change or delete answers already delivered.

## HTTP and trust boundary

Only `/`, `/api/state`, `/api/notes`, `/api/markdown` and published report routes are exposed. `/api/state` carries each note's `ack_kind` and `ack_text`, plus `ack_html` when a reply was rendered, and the reports in `seq` order with each report's `seen_at`; it never carries report submissions. `/api/markdown` accepts a bounded, token-protected draft and returns read-only HTML without writing any inbox record. It reuses the optional renderer; a server without one never starts, so a live page always renders. The Write / Preview control is not a WYSIWYG editor. Confirmation stays beside Send, without an extra message block, and the last sent text becomes the empty textarea's placeholder with no label prefix. Published reports support `/api/reports/<id>/html`, `/source` and token-protected `/api/reports/<id>/submit` and `/api/reports/<id>/seen`, the last of which is the browser's read stamp and the only report route the browser writes for a reason other than answering. `POST /api/save-state` takes the page's cached copy of the log and the tasks and writes them as `saved-state.ndjson` at the repository root, untracked and outside the state directory, so the copy outlives the directory a restore drops (report submission `c0fcfad9`); the owner's report answers are appended from the database rather than from the page, so an answer the owner sent comes back instead of being typed again (note `120fe358`), and a note line keeps its `task_id`. One file, three kinds of line, and the two importers split them. No endpoint accepts arbitrary filesystem paths; only the agent's CLI can register a report. The browser cannot acknowledge messages. POST requires JSON, a bounded body and a per-process token. The bound is per route rather than one number for every POST, on a review of `6b308e3` which found that a single 32 KiB limit made the documented submission maximum unreachable over HTTP: notes and rendered Markdown take 32,768 bytes and a report submission takes 1,000,000, being what 150,000 characters cost once each one is escaped as `\uXXXX`. Both are checked before the body is parsed, and the character bound on a submission is unchanged and still applied after it. A `task-import --replace` is one transaction for the same reason: every record is validated before anything is written, so an invalid one costs nothing rather than taking the existing list with it. `publish` refuses a report whose fields fail validation on the same principle and with the same history: one stored first used to break its own `/html` route at render time, and because `do_GET` caught `OSError`, `sqlite3.Error` and `RuntimeError` but not the `ValueError` that validation raises, the connection closed with no response at all — the browser saw a failed fetch and the tab had nothing to explain it with. That route is guarded now and answers 500 with a JSON `error`, and `/source` still serves the Markdown, so the agent can read what the browser could not render. This is CSRF resistance, not authentication: anyone with preview access can read the page and obtain that token.

Treat the preview URL as private session access. Do not publish secrets. Do not enable CORS, arbitrary file serving or remote assets. Raw HTML in Markdown is disabled; renderer URL validation and the content policy constrain active content. Report titles and messages are text, not HTML. The server accepts the Arena proxy host and does not block iframe embedding.

## Observed transition — 2026-09-20, Asia/Manila

The owner requested the rename from `arena-live-steering` to `arena-preview-steering`, the new reporting skill and the shared tabbed page. The experiment persisted user notes while the agent was idle and delivered notes during continued work after a blocking question revealed the preview. The owner found this more usable than the prior ntfy channel. This establishes session-level evidence, not a guarantee about all clients, sandbox restarts or future Arena versions.

The owner reported that Arena's native file viewer exposed raw Markdown only after local commits. Until this migration, the reporting workflow therefore force-added ignored reports into a local `chore(reports): hold the local records` commit after real commits were pushed, never pushed that records commit, and undid it next turn. This workaround preserved visibility, not rendered Markdown. It is retired by explicit approval, not erased from history.

The old steering workflow used an external ntfy topic, agent-side page-fetch polling and a local ingestion log. Sandbox HTTP access to ntfy had produced misleading empty responses, while page-fetch could read it. Topic caching, response mangling and polling made that path fragile. The owner explicitly retired it during this session. The old instructions and implementation remain in Git history before the preview migration; they are historical, not active fallback instructions.

The owner explicitly warned that the preview may not be permanent. If it fails, report the observed failure and ask how to continue through ordinary chat. Do not silently resume ntfy, force-add report artifacts or claim a replacement was authorized forever. A future change can select another transport with fresh evidence and approval.

## Recovery and checks

- Preserve the ignored state directory and Markdown sources when restarting. Process IDs, venv packages and URLs are not durable; restore the approved renderer and restart the same state directory as needed.
- Reload an old browser page after server restart to obtain the new submission token. Keep/copy an unsent draft first if browser storage is unavailable. Browser drafts are origin-local, not a cross-device backup.
- If a port is occupied, identify its owner or select another port; never kill an unrelated service. A failed read or save must remain visible, not become an empty state.
- Run `python <skill>/scripts/check_preview.py` with `markdown-it-py` available. It checks missing state, persistence, concurrent retry deduplication, receipt transactions, multiple reports, Markdown fields and their answer submissions, unsafe Markdown, absent-renderer behavior, validation, the retired form and export routes and HTTP route boundaries.
- Import `saved-state.ndjson` soon after the owner presses save state. The owner moved it to the repository root on a later answer to the same report (`save-state-home`), changing their first choice of the state directory, so a restore keeps the file and the import closes the rest of the window. The file carries the notes, the tasks and the owner's report answers; a report itself rebuilds from the draft that produces it.
- Rebuild the queue from Git when the task backup dies with the state directory, which is what a restore does to both at once: `git log --oneline` is one finished task per shipped change, with the commit as its detail line, and `task-import` reads the JSON that those records form. A restore in this session deleted the backup and the plugin list together, and the queue came back inside one turn that way.
- Import a pasted log with `import-notes`, and never assume an answer: a line keeps the receipt it carries and a line without one stays unacknowledged. The one restore that marked twenty-eight pasted notes as answered taught this the hard way, because the paste held the owner's unacknowledged messages and five of them had never been answered.
- The client holds the write token from the page it loaded, so a server restart refuses every write until the owner reloads. `/api/state` hands the page the token on every poll, so the poll keeps the write token fresh and a save pressed after a restart lands with no refresh at all; `request()` also takes the token from a fresh page and retries once on a 401 or 403, and its reader accepts either quote style, because the shipped page is minified and the owner's presses after a reset had landed nowhere until a manual reload (note `2647459f`). It waits a second and retries once when a write never reached a server at all. The owner pressed save state several times after a sandbox reset with nothing landing until they refreshed and pasted the log by hand (note `2647459f`), which is the failure this closes.
- Log text renders without `breaks`: its paragraphs inherit `white-space: pre-wrap` from `.message p`, so the newline the source carries is the line break and a `<br>` beside it doubled every gap (owner note `0d1d1123`). The draft preview keeps `breaks=True` for the same reason. A paragraph carries no `<br>` anywhere, on the owner's suggestion in the same note: markdown-it turns a newline into one when breaks are on, and a paragraph now keeps its source newlines instead, while a break inside another element, a list item for one, stays as markdown-it wrote it.
- The distributed copies ship a minified build of `assets/app.js`, `assets/style.css` and `assets/index.html`, on the owner's answers to report `minification-scope`: those three types only, a pinned third-party minifier per language, and a size budget per file in the root README table. The readable baselines stay in `skills/refs/arena-preview-steering/assets/`, and Python and Markdown stay readable everywhere. `maintenance/minify.py` builds both copies, and `check.py` gates each live asset through the README budget. The build refuses any warning from a minifier: a stray `"` in the stylesheet once made clean-css drop every rule it swallowed, including `#log-filter`, with a warning, exit 0 and a smaller file, which is both why the log filter kept the wrong height (owner notes `37b4069f` and `6da88604`), which the last pass closed by dropping the select's native chrome so its box is built like the buttons beside it, and why a size budget alone is not a build proof. `check_preview.py` pins exact page strings, so CI runs it from the refs baseline, while the client harness runs against the shipped live build, which is why a minified `app.js` still has to pass it.
- Check `assets/app.js` with `node --check` and run `node scripts/check_client.cjs` where Node is available. The latter uses a minimal simulated DOM to check theme defaults/persistence, tabs, drafts, Enter/IME, retries, receipt display and report-field answers. These checks do not prove actual browser rendering, focus behavior, storage or iframe visibility; record manual browser observations separately.
- Migration of the first experiment: stop its server, import its final `notes.ndjson`, explicitly acknowledge only IDs already acknowledged in chat, and start this server with the new state directory. Keep the old file until verified; do not delete user messages to migrate.

Browser attachment links returned HTTP 200 with attachment headers during this session, but the owner observed no download in the Arena sandbox preview. The exact browser restriction was not identified. By explicit owner choice, the UI offers no download/source controls, and the standalone HTML export was removed on 2026-09-20 because it never worked in that preview. The Markdown source and the live Reports tab are the delivery path; never claim embedded downloads work.

The JSON form path — `publish-form`, the `forms` table, `/api/forms/<id>` and `/api/forms/<id>/submit`, the Forms tab and the `FORM <id> <title>:` note header — was removed on 2026-09-20 by explicit owner choice, once report fields covered the same three control types with rendered Markdown context around them. A questionnaire is now a report whose source is only field markers, and every answer arrives as a `REPORT` note. `/api/forms/*` returns 404; do not document, publish or promise a form.

The shared renderer opens non-fragment Markdown links in a new tab with `noopener noreferrer`, so report, draft and log links do not navigate the preview. Fragment links stay in place. Browser popup restrictions remain outside the server's control.

Acknowledgements moved into the log on 2026-09-20 at the owner's request: `ack` now requires `--reply <markdown>` or `--note <text>`, the reply renders with the same message CSS as a user note, and each log entry shows its ID so the owner can match it to an agent acknowledgement. With a visible preview that in-log answer replaces the chat `ACK:` line; chat acknowledgement remains the fallback when no preview is visible. The former bare `ack <ids>` form is gone: an acknowledgement without an answer text is rejected.

Report answers left the message log on 2026-09-20 at the owner's request: the `✓ Sent` receipt in the Reports tab is the user's confirmation, and answers now live in their own table so the log shows only what the user wrote. Receipts lead with the ID's first seven characters in a monospaced `code` element, on a follow-up note that wanted it to look cleaner, and carry the whole ID in that element's tooltip so it can still be read without 36 characters on every line. A receipt reads as parts separated by one ASCII dot: the short ID, the writer when the agent wrote the line, the state dot, the delivery time, then `Task added` when a task answers that message. The owner asked for the dot after the ID by name (notes `69dcf681` and `7b183104`), and `Task added` carries the task ID in its title, because a read line with no marker is indistinguishable from a dropped one (note `fe00a32d`). The log copy carries `task_id`, so a restore keeps the link. That element takes three modifiers, the third on the owner's note `96103c05`: a plain click copies the seven characters on show, a shift-click copies the whole ID, and a ctrl-click quotes the note back instead of copying anything, filling the composer with `RE: <shortid>` and a newline and landing the caret after it. Meta counts as ctrl, being the same gesture on a Mac. Quoting opens a hidden composer, keeps a draft already written below a blank line rather than losing it, saves what it writes as the draft, and retargets a first line that is already a quote, so quoting a second note replaces the prefix instead of stacking two. An intermediate version printed the full ID with a hyphen after the seventh character and was reverted the same hour on the owner's bug report that the log showed the full ID where seven were expected, then a state dot and the delivery time in the receipt's muted face, as in `3882b26 ● · Sep 21, 00:27`: the dot is gray while the agent has not read it, blue once a CLI `read` has stamped `seen_at`, and green once the agent has acknowledged it in either kind, with the word — Sent, Seen or Said — carried in the dot's `title` and `aria-label` rather than printed in the line. The log also carries a filter that selects over those same three states — All, Sent, Seen and Said — with the choice saved in browser storage, a filter that matches nothing saying so and counting what it is holding back, the filter itself sitting immediately left of the copy button in the log's button row and sharing that row on every width, on owner notes `1006cb38` and `7f52e5fe`, the filter box the same height as the buttons beside it on owner note `37b4069f`, and the log's own place kept: at its end it returns there when rows come back, because the browser clamps the scroll while the view is short and never puts it back, and scrolled away the owner keeps the position they chose; every icon button carries a hover title that repeats its accessible name, on owner note `bef51970`, and the copy button stays whole-log, because it is the restore path and a filtered copy would restore a partial log as if it were all of it. A log longer than its panel carries a jump bar over the bottom of its scroll area: a strip rather than a round button, on owner direction, shown only while the log is scrolled away from its end and taking it back to the newest message when it is used; it is as wide as its label rather than the pane, on owner note `edbdfcae`. The header also carries a save button, `⤓`, in the top bar beside the theme button rather than in the log's own row, on owner note `0f27a2b6`, which posts the page's cached copy of the log and the tasks to the server; that cache is refreshed on every successful poll, so the button still has something to write after a wipe has emptied the server, and its receipt names the counts and the path it wrote. A shift-click copies that cached state as minified JSON instead of posting it, on owner note `586d52f7`, on the same direction that made the old JSON copies minified: the agent parses them and indentation only costs tokens. The button's own name carries the gesture, and a copy restores the name it came in with rather than a wording the copy invented, so the save button still says what a plain click does. Three states on owner direction, which replaced the printed `Sent`/`Seen`/`Said` words, and before those the earlier `Delivered`/`Seen` pair — which had itself dropped the `ACK-ed`/`Awaiting ACK` pair, the separate acknowledgement timestamp and the `id` label so the line fits narrow screens. A message carries a small uppercase `agent` tag when the agent wrote it, which is what the owner could not tell on note `d6fcfc2a`; the agent writes no messages of its own, on the owner's answer in report submission `e43d42a8`: it answers the owner's notes and starts none, so the tag marks only what predates that rule, and it survives a save and restore because a note line keeps its `origin`. Only the CLI's `read` stamps `seen_at`, never the browser's `/api/state` poll, and an acknowledgement preserves an earlier stamp, so Seen means the agent looked at the note rather than that some client fetched the list. The receipt is therefore two child elements rather than one text node, which is why the harness stub reads `textContent` through to children. `Seen` therefore carries the delivery time and not the acknowledgement time, which the receipt no longer shows anywhere. The last-check line beside the connection dot reads `Last checked <time>`, or `Not checked yet.` before the first read, and no longer names the agent. Prose refers to a note by the first seven characters of that ID, the prefix the receipt shows, and never by its sequence number, on owner direction: an ID is stable across a rebuilt state file and `ack` takes the full one, while `seq` is display order only. A message in the log reads in three parts and in this order, on owner direction: the user's own text, then the agent's answer, then the receipt line, so a reply sits under the note it answers and the small muted receipt closes the block. The answer takes 8px above and the receipt 8px above that, where before the swap the answer carried 10px and the receipt none; a hidden answer still holds its place in the DOM, so the receipt's gap never collapses against the message text. Reports are the exception the owner asked for earlier — the select numbers them `1. Title` by publish order, because a report is cited by title and the number only fixes the order. Existing REPORT notes written before the move stay in the log as history.

`serve` began refusing to start without `markdown-it-py` on 2026-09-20 at the owner's request, replacing the raw-text fallback and the banner idea that preceded it: the startup error names the install command, and `read`, `ack` and `publish` keep working so steering survives a broken venv.
